var.root = var.CWD + "/static/"

server.name = "calgary.sh"
server.document-root = var.root
server.errorfile-prefix = var.root + "status/"
server.port = 8080

index-file.names = ( "resume.html" )

mimetype.assign = (
  ".html" => "text/html", 
  ".css" => "text/css",
  ".txt" => "text/plain",
  ".jpg" => "image/jpeg",
  ".png" => "image/png",
  ".tiff" => "image/tiff"
)
